# backlog

Execution plans from backlog audit (`reference/backlog-audit.md`). Implement via factory loop — not chat-only.

## Deferred (no plans written yet)

From the loop ladder adoption (`decisions.md` 2026-06-12) — recorded so a future audit can pick them up; plans get written on selection, per `reference/execution-plan.md`:

- ~~`omakase status`~~ — SHIPPED 2026-06-12 (read-only; see `decisions.md`). `omakase loop` (a built-in runner) stays deferred — the motor is BYO.
- Automated upshift proposals from gate history — needs a gate-history ledger first; `omakase status` is the natural home when it lands
- L4 fleet loop spec — named horizon only in `reference/loops.md`
