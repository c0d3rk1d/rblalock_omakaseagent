# Dependencies
node_modules/

# OS / editor junk
.DS_Store
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Temporary test projects (dogfooding harnesses)
omakase-dogfood-*
/tmp/omakase-*

# Harness installs at repo root (from `omakase init` / `skills install`).
# Anchored with / so dist/<harness>/.xxx/ bundles in dist/ stay committed.
/.cursor/
/.claude/
/.grok/
/.opencode/
/.codex/
/.agents/
/.omakaseagent/

# Environment / secrets
.env
.env.local
*.pem
*.key

# dist/ — committed (built bundles for npm install)
# AGENTS.md — committed (example project pointer; optional for this repo)